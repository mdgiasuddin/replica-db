This application supports db-replication where all the write operation will be happened in the primary db and read operation will be happened in the replica db.
### Project Setup:
Run Command:
```angular2html
sudo docker compose up -d
```
Run the project.